# =============================================================================
# Dutchkem Trading AI — Production Environment Variables
# =============================================================================
# Used by Render (env.production.example) and other production hosts.
# Render: copy this to .env or paste values into the Render dashboard.

# --- Django Core ----------------------------------------------------------------
SECRET_KEY=generate-a-50-char-random-string
DEBUG=0
DJANGO_SETTINGS_MODULE=config.settings_production

# --- Database -------------------------------------------------------------------
# Render auto-injects DATABASE_URL when linked to a PostgreSQL database.
USE_SQLITE=0

# --- Redis / Celery -------------------------------------------------------------
# Create manually in Render dashboard → Redis → paste Internal Redis URL here.
REDIS_URL=redis://your-redis-url:6379/0
CELERY_BROKER_URL=redis://your-redis-url:6379/0

# --- MetaTrader 5 (SYNX-MT5-MCP bridge) ----------------------------------------
MT5_HOST=your-mt5-bridge.your-domain.com
MT5_PORT=443
MT5_WS_PORT=443
MT5_TIMEOUT=10
MT5_MAX_RETRIES=5
MT5_RETRY_DELAY=2.0

# --- Cloudinary (optional — media storage) --------------------------------------
CLOUDINARY_URL=cloudinary://api_key:api_secret@cloud_name

# --- Production Security --------------------------------------------------------
SECURE_SSL_REDIRECT=1
SESSION_COOKIE_SECURE=1
CSRF_COOKIE_SECURE=1
ALLOWED_HOSTS=dutchkem-trading-ai.onrender.com
CORS_ALLOWED_ORIGINS=https://dutchkem-trading-ai-frontend.onrender.com
