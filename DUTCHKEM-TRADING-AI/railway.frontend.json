{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "dutchkem-trading-ai/frontend/Dockerfile.railway"
  },
  "deploy": {
    "startCommand": "npm install && npm run build && npx serve -s build -l $PORT"
  }
}
